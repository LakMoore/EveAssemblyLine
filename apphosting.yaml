# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  maxInstances: 1
  # concurrency: 80
  # cpu: 1
  memoryMiB: 1024

# Environment variables and secrets.
# DO NOT store secrets in this file. Use Cloud Secret Manager instead.
# This file is checked into source control and can be read by anyone with access to the repository.

  # Grant access to secrets in Cloud Secret Manager.
  # You can create secrets with the CLI command `firebase apphosting:secrets:set <secretName>`.
  # See https://firebase.google.com/docs/app-hosting/configure#secret-parameters
  # - variable: MY_SECRET
  #   secret: mySecretRef
scripts:
  buildCommand: npm run sde:prepare && npm run build